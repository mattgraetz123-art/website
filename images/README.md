# Images needed

Place the following brand assets in this folder (filenames must match exactly, referenced from index.html / style.css):

- `hero-rain.jpg` — B&W photo of runners' feet on a wet road (used full-bleed in the hero section and as a product card texture).
- `run-club.jpg` — dark silhouette group running photo (used as the Run Club section background and a product card texture).
- `logo-vertical.png` — stacked vertical "444" outline logo with "RUNNER" wordmark (used in the About section). Export with a transparent background.
- `logo-badge.png` — horizontal "444 RUNNER" badge mark (optional, for use in nav/footer if you want the graphic version instead of the text logo).

Until these are added, the page will show broken image icons where photos are referenced — the layout and CSS background colors still work fine without them.
